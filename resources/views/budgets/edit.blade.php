<x-app-layout><div class="d-flex justify-content-between align-items-center mb-4"><h1 class="page-title">Edit Budget</h1><a class="btn btn-light" href="{{ route('budgets.index') }}">Back</a></div><div class="card p-4" style="max-width:900px"><form method="POST" action="{{ route('budgets.update', $budget) }}">@csrf @method('PUT')<div class="row g-3"><div class="col-md-6"><label class="form-label">Budget name</label><input class="form-control" name="name" value="{{ old('name', $budget->name ?? '') }}" required></div><div class="col-md-6"><label class="form-label">Category</label><select class="form-select" name="category_id"><option value="">All categories</option>@foreach($categories as $c)<option value="{{ $c->id }}" @selected(old('category_id', $budget->category_id ?? '') == $c->id)>{{ $c->name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Amount</label><div class="input-group"><span class="input-group-text">{{ auth()->user()->currency }}</span><input class="form-control" type="number" min="0.01" step="0.01" name="amount" value="{{ old('amount', $budget->amount ?? '') }}" required></div></div><div class="col-md-6"><label class="form-label">Alert at (%)</label><input class="form-control" type="number" min="1" max="100" name="alert_percentage" value="{{ old('alert_percentage', $budget->alert_percentage ?? 90) }}" required></div><div class="col-md-6"><label class="form-label">Start date</label><input class="form-control" type="date" name="start_date" value="{{ old('start_date', isset($budget) && $budget->start_date ? $budget->start_date->format('Y-m-d') : today()->startOfMonth()->format('Y-m-d')) }}" required></div><div class="col-md-6"><label class="form-label">End date</label><input class="form-control" type="date" name="end_date" value="{{ old('end_date', isset($budget) && $budget->end_date ? $budget->end_date->format('Y-m-d') : today()->endOfMonth()->format('Y-m-d')) }}" required></div><div class="col-12"><div class="form-check"><input class="form-check-input" type="checkbox" name="is_recurring" value="1" id="isRecurring" @checked(old('is_recurring', $budget->is_recurring ?? false))><label class="form-check-label" for="isRecurring">Repeat this budget every month automatically</label><div class="form-text">A new budget with the same name, category, and amount will be created for next month once this period ends.</div></div></div></div><div class="mt-4"><button class="btn btn-primary">Save Budget</button></div></form></div></x-app-layout>
