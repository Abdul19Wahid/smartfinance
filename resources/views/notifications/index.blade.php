<x-app-layout><div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="page-title">Notifications</h1><div class="small-muted">Budget alerts and system messages.</div></div><form method="POST" action="{{route('notifications.read-all')}}">@csrf @method('PATCH')<button class="btn btn-light">Mark all read</button></form></div><div class="card p-3">@forelse($items as $n)<div class="p-3 border-bottom d-flex justify-content-between gap-3 {{!$n->is_read?'bg-light':''}}"><div><div class="fw-bold">{{$n->title}}</div><div class="text-muted">{{$n->message}}</div><div class="small-muted mt-1">{{$n->created_at->diffForHumans()}}</div></div>@if(!$n->is_read)<form method="POST" action="{{route('notifications.read',$n)}}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-primary">Mark read</button></form>@endif</div>@empty<div class="p-5 text-center text-muted">No notifications.</div>@endforelse{{$items->links()}}</div></x-app-layout>